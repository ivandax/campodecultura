import styled from "styled-components";

export const RequestPasswordWrapper = styled.form`
  width: 280px;
  display: flex;
  flex-direction: column;
  padding: 24px;
  gap: 8px;

  input {
    padding: 4px;
  }
`;
