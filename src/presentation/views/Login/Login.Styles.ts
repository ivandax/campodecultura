import styled from "styled-components";

export const LoginFormWrapper = styled.form`
  width: 280px;
  display: flex;
  flex-direction: column;
  padding: 24px;
  gap: 8px;

  input {
    padding: 4px;
  }
`;
